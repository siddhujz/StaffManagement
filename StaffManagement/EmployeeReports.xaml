<Page x:Class="StaffManagement.EmployeeReports"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Employee Reports">

    <Grid Style="{StaticResource gridStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" Style="{StaticResource blueButtonStyle}" Click="performance_Report_Button_Click">Performance Report</Button>
        <Button Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" Style="{StaticResource blueButtonStyle}" Click="qa_Trending_Button_Click">QA Trending</Button>
        <Button Grid.Column="2" Grid.Row="0" HorizontalAlignment="Center" Style="{StaticResource blueButtonStyle}" Click="monthly_Qualitivity_Button_Click">Monthly Qualitivity</Button>

    </Grid>

</Page>
